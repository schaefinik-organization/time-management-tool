import {describe, expect, it} from 'vitest'

describe('Initiale Tests', () => {
    it('sollte prüfen, ob das Test-Framework korrekt läuft', () => {
        const isReady = true
        expect(isReady).toBe(true)
    })
})